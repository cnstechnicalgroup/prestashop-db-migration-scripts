PrestaShop DB Migration Scripts
===============================

The project aims to catalog DB upgrade scripts to assist with automated PrestaShop upgrades across environments (dev, test, stage, prod).


